const Loading = () => {
  return (
    <div className="w-full h-screen flex justify-center items-center">
      <p className="font-semibold text-5xl text-black">Loading...</p>
    </div>
  );
};

export default Loading;
